name: Test
on:
  push:
    branches:
      - master
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy on gh pages
        uses: actions/checkout@v2
      - run: npm ci & ng deploy
